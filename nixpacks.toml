# nixpacks.toml is NOT used by Railway (Railway uses Railpack, not Nixpacks).
# Python + matplotlib are installed via package.json postinstall script instead.
# This file is kept as documentation only.
